import styled from 'styled-components'
import { colors } from '../../styles'

export const ContainerWorkSpace = styled.div`
  margin: 0 auto;
`

export const Title = styled.h1`
  padding: 30px;
  text-align: center;

  span {
    background-color: ${colors.white};
    padding: 4px 16px 4px 16px;
    border-radius: 6px;
  }
`
